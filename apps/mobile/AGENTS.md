# Expo HAS CHANGED

This project is deliberately pinned to **Expo SDK 54** (not the latest SDK) to stay version-compatible with `kmkb-mobile-app`, the existing grocery app this project will absorb in a later phase (see `D:\A16Z\zentrip-feature-specs\05-india-services-layer-grocery-integration.md`). Do not run `expo upgrade` or bump the `expo` package without updating that plan first.

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
