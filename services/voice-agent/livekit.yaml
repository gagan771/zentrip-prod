port: 7880

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true

redis:
  address: redis:6379

# Replace with LIVEKIT_API_KEY / LIVEKIT_API_SECRET from .env before production.
keys:
  devkey: secret
